# Core
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
SQLAlchemy==2.0.36
alembic==1.14.0
aiosqlite==0.20.0
asyncpg==0.30.0
psycopg2-binary==2.9.10
greenlet==3.1.1
httpx==0.28.1
anyio==4.7.0
PyJWT==2.10.1
cryptography==44.0.0
python-multipart==0.0.20
email-validator==2.2.0
arq==0.26.1
redis==5.2.1

# --- Real path (x402 payments + Hermes MCP). Installed by default so the
# real providers are ready the moment you set the env keys. The [svm] extra
# pulls in solders/solana for REAL on-chain Solana settlement (not optional
# here — the platform settles on solana:mainnet). ---
x402[svm]==2.10.0
eth-account
mcp

# Dev / test
pytest==8.3.4
pytest-asyncio==0.25.0
